.DS_Store
Thumbs.db
node_modules
__pycache__/
*.pyc

# Build output for the public site. Regenerated by build-offline.py on every run, and
# pushed to the public repository rather than kept here.
dist/

# Local D1/Miniflare state from `wrangler dev --local`, used only to test worker/maaser-worker.js
# against a local copy of the database. Never the shul's real data.
worker/.wrangler/
